"use client";

import React from "react";
import Layout from "../../../components/common/Layout";

export const Dashboard = () => {
    return (
        <Layout>
            {" You're a services seeker now "}
        </Layout>
    );
};

export default Dashboard;